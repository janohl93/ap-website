# Logo placement

Replace `logo.svg` in this folder with your final logo file (SVG or high-resolution PNG). The header automatically loads `assets/logo/logo.svg`, so keeping the same file name will update the brand mark across the site.
